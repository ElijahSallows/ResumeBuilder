@using ResumeBuilder.Shared.Models;
@inherits ResumeListView<SocialLink>;

<h2 class="section-header">
    <span>Socials</span>
    <button class="oi oi-plus" @onclick="@OnAddClicked"></button>
</h2>

@foreach (var social in List)
{
    @* At some point, this can be improved. Since only the links are shown at present, this is sufficient.*@
    <div class="social vertical-margins">
        <DeletableInputText T="SocialLink"
                            OnUpdate="@OnUpdated"
                            OnDelete="() => Delete(social)"
        @bind-Value="social.Url" />
    </div>
    @*<div class="input-wrapper list-wrapper">
        <label for="social-url">
            <span class="label">URL</span>
        </label>
        <InputText class="input bordered input-text" placeholder="Type here..." id="social-url" @bind-Value="social.Url" />
    </div>*@
}

@code {

}
